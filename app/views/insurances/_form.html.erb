<%= form_for([@insurance]) do |f| %>
  <% if @insurance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@insurance.errors.count, "error") %> prohibited this insurance from being saved:</h2>

      <ul>
      <% @insurance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Name" %><br />
    <%= f.text_area :name ,rows:2%>
  </div>
  <div class="field">
    <%= f.label "Adresse" %><br />
    <%= f.text_area :address, rows:4%>
  </div>
  <div class="field">
    <%#= f.label Nummer: %><br />
    <%#= f.text_field :number %>
  </div>
  <div class="row-fluid">
    <div class="actions span2">
      <%= f.submit 'Speichern',class:"btn btn-block btn-primary" %>
    </div>
  </div>
<% end %>
